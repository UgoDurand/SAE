{% extends 'base.html.twig' %}

{% block title %}Ajouter un membre{% endblock %}

{% block body %}
    {{ form_start(equipeForm) }}
    {{ form_row(equipeForm.Prenom) }}
    {{ form_row(equipeForm.Nom) }}
    {{ form_row(equipeForm.Linkedin) }}
    {{ form_row(equipeForm.imageFile) }}

    <button type="submit" class="btn">Ajouter</button>
    {{ form_end(equipeForm) }}
{% endblock %}